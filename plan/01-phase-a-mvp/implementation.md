## 合同审计
- 主界面上传合同并调用合同审计 API
- 解析正文文本，PDF 文本不足时触发 OCR 回退
- 输出摘要与风险清单

## 法规导入（Admin）
- 支持 doc/docx/pdf
- 解析后按条款切分入库
- 保存元数据：法规类型、生效日期、适用区域

## 模型调用
- LLM 参数集中配置（端点/模型/温度/最大 token 等）
- 统一封装调用与错误处理

## 审计引擎
- 基础规则 + 相似度匹配
- 输出风险类型、证据条款、建议
# 阶段 A 实现思路

## 法规导入
- 支持 doc/docx/pdf
- 解析后按条款切分入库
- 保存元数据：法规类型、生效日期、适用区域

## 合同解析
- 提取正文文本 + 基础结构
- 输出摘要用于人工校验

## 审计引擎
- 基础规则 + 相似度匹配
- 输出风险类型、证据条款、建议

## 批注输出
- 使用 python-docx 写入批注
- 批注内容结构固定：风险类型 + 证据条款 + 建议