# 阶段 A 设计方案

## 核心模块
- 合同审计：主界面上传合同并生成审计摘要与风险清单
- 法规管理：Admin 内法规导入与条款切分
- OCR 引擎：扫描 PDF 回退 OCR，配置驱动引擎选择
- 模型调用：LLM 参数集中配置与调用封装
- 审计引擎：法规匹配 + 基础风险规则

## 业务流程
合同上传 → 内容解析（含 OCR 回退）→ 法规匹配 → 风险识别 → 审计结果输出
法规导入与检索在 Admin 页面独立流程完成

## 关键约束
- 审计意见必须能定位到法规条款
- 输出文件须与 Word 兼容