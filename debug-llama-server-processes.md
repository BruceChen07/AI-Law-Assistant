# [OPEN] llama-server-processes

## 问题描述
- 现象：系统中同时出现多个 `llama-server.exe` 进程，用户怀疑是否异常。
- 目标：诊断这些进程是否属于 Ollama 的正常模型运行机制；如果异常，定位根因并修复；如果正常，给出明确解释。

## 已知上下文
- 项目：`AI-Law-Assistant`
- 环境：Windows，本地 Ollama，历史上已经接入多模型与长超时重试链路。
- 用户截图显示：存在多个 `llama-server.exe`，且内存占用较高。

## 待验证假设
1. Ollama 为不同模型或不同并发会话分别拉起独立的 `llama-server.exe`，当前属于正常现象。
2. 应用最近引入的长超时/重试逻辑导致相同请求被重复触发，间接造成多个 `llama-server.exe` 持续驻留。
3. 管理后台 `llm-test` 与首页合同审计并行调用不同模型，导致 Ollama 同时加载多个模型实例。
4. Ollama 的 `keep_alive` 或默认模型缓存策略未及时释放模型，导致旧的 `llama-server.exe` 未退出。
5. 存在异常故障恢复或重复拉起问题，某个上游服务在请求失败后重复创建推理子进程，属于不正常状态。

## 调试计划
1. 采集运行态证据：进程树、命令行、端口占用、父子关系、模型列表。
2. 结合项目日志核对最近是否有并发审计、重试或多模型切换。
3. 根据证据判断是正常机制还是异常拉起。
4. 若异常，再做最小修复并验证。

## 当前状态
- 状态：进行中
- 结论：待采集证据后确认

## 运行态证据
- `ollama app.exe` PID `43432`，`ollama.exe serve` PID `38432`，当前服务端口 `11434`。
- 存在 4 个 `llama-server.exe`：PID `43940` / `1736` / `39576` / `38940`，分别监听 `2112` / `13088` / `10502` / `10543`。
- 这些 `llama-server.exe` 的启动时间分别为 `01:18` / `01:39` / `01:46` / `01:46`，而当前 `ollama.exe serve` 启动时间为 `01:50`，时间线不一致。
- `ollama ps` 返回空列表，说明当前 Ollama 管理面并不认为有模型正在活动。
- 4 个 `llama-server.exe` 的父进程均已不存在，属于孤儿进程。

## 假设验证
1. 正常多模型/多会话拉起：否。若正常，`ollama ps` 应能看到活动模型，且应挂在当前 `ollama.exe serve` 之下。
2. 重试导致重复提交：部分相关，但不是直接根因。历史上频繁切换与重试可能诱发更多模型实例，但当前异常点是这些实例已脱离 Ollama 主进程。
3. 前后台并发加载不同模型：历史上成立，日志中确有 `qwen3:4b` / `qwen3.6:27b` / `gemma4:12b` 切换与并发调用。
4. `keep_alive` 缓存未释放：不能完全解释。正常缓存仍应受 `ollama ps` 管控，不应出现父进程消失的孤儿实例。
5. 异常重复拉起或服务重启遗留：成立。当前最符合证据。

## 当前结论
- 结论：不正常。
- 根因判断：此前模型切换、长时任务和/或 `ollama.exe serve` 重启后，旧的 `llama-server.exe` 未被正常回收，遗留为孤儿进程，持续占用大量内存。

## 处置动作
- 已执行：终止孤儿进程 PID `43940` / `1736` / `39576` / `38940`。
- 未修改：项目业务代码未改动。

## 修复后验证
- 当前仅剩 `ollama app.exe` 与 `ollama.exe serve`。
- 当前监听端口仅剩 `9817`（ollama app）与 `11434`（ollama serve）。
- `llama-server.exe` 数量已降为 `0`。
- `ollama ps` 为空，和当前“没有活跃模型加载”的状态一致。

## 最终状态
- 状态：已修复
- 建议：若后续再次出现同类现象，应优先检查是否发生 `ollama.exe serve` 重启、模型频繁切换或长时任务中断，必要时先清理孤儿 `llama-server.exe` 再继续排查上游触发源。
